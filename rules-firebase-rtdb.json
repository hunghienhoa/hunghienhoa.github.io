{
  "rules": {
    ".read": true,
    ".write": false,
    "apps": {
      "$appId": {
        "views": {
          ".read": true,
          ".write": true,
          ".validate": "newData.isNumber() && (data.exists() ? newData.val() == data.val() + 1 : newData.val() == 1) && newData.val() >= 0"
        },
        "downloads": {
          ".read": true,
          ".write": true,
          ".validate": "newData.isNumber() && (data.exists() ? newData.val() == data.val() + 1 : newData.val() == 1) && newData.val() >= 0"
        }
      }
    }
  }
}

